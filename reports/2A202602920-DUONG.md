# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Nguyễn Minh Dương
- Mã học viên: 2A202602920
- Nhóm: Miniature
- Repository/branch: nmduong

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Thu thập và chuẩn hóa dữ liệu | Thu thập 3 văn bản, 5 bài viết và chuyển sang Markdown | `data/`, `src/task1_*`–`task3_*` | Done |
| RAG pipeline | Chunk/index, dense, BM25, RRF, fallback và generation có citation | `src/task4_*`–`task10_*` | Done |
| UI và evaluation | Streamlit source highlighting, 15 golden cases, A/B 4 metrics | `app.py`, `src/evaluate.py`, `group_project/evaluation/` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Dùng Gemini cho cả embedding và generation.
   **Lý do/evidence:** Một provider nhất quán, API key đã được nhóm cấu hình; end-to-end smoke test trả citation đúng.
   **Trade-off:** Free-tier có rate limit nên indexing cần batching/backoff.

2. **Quyết định:** Giữ hybrid + RRF cho sản phẩm nhưng báo cáo trung thực dense-only thắng nhẹ.
   **Lý do/evidence:** A/B average 0.853 so với 0.848; hybrid có faithfulness tốt hơn nhưng precision thấp hơn.
   **Trade-off:** Hybrid bền với từ khóa/tên riêng nhưng có thể đưa thêm context nhiễu.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest -q`; query Cam Ranh và query ngoài miền về Bitcoin.
- Kết quả trước/sau nếu có: 20/20 tests pass; in-domain trả 35 km kèm `[1]`; out-of-domain safe-refuse.
- Lỗi đã phát hiện và cách xử lý: model embedding cũ trả 404, đổi sang `gemini-embedding-001`; quota 429 được xử lý bằng batch/backoff.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: golden set còn nhỏ và Gemini tự chấm có nguy cơ self-judge bias.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: thêm reranker/cutoff sau RRF và mở rộng bộ out-of-domain calibration.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-20
- Tên thành viên: Nguyễn Minh Dương
