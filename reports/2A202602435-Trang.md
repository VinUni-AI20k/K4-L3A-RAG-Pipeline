# Individual contribution report

## Thông tin

- **Họ và tên:** Nguyễn Thu Trang
- **Mã học viên:** 2A202602435
- **Nhóm:** Miniature
- **Repository/branch:** `K4-L3A-RAG-Pipeline` / branch `trangnguyen`
---

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Streamlit UI & Parameter Tuning | Tinh chỉnh giao diện Chatbot UI; bổ sung bộ điều khiển tham số trên Sidebar; sửa lỗi hiển thị `DeltaGenerator` của Streamlit Magic; cập nhật nhận tham số linh hoạt cho `generate_with_citation()`. | `app.py`, `src/task10_generation.py`, `tests/test_contracts.py` (Commit `b6e74bd`) | Done |

---

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Đưa các tham số truy xuất lên Sidebar của Streamlit UI và truyền động vào pipeline `generate_with_citation()`.  
   - **Lý do/evidence:** Giúp tester/người dùng có thể trực tiếp tinh chỉnh và quan sát ảnh hưởng của số lượng context (`top_k`), ngưỡng fallback Cosine (`score_threshold`) và thuật toán RRF Reranking ngay trên UI mà không cần sửa code.
   - **Trade-off:** Sidebar có nhiều widget hơn, cần bổ sung tooltip hướng dẫn để người dùng hiểu rõ ý nghĩa từng tham số.

2. **Quyết định:** Khắc phục lỗi hiển thị đối tượng `DeltaGenerator` bằng cách chuyển biểu thức inline sang khối `if/else` tường minh.  
   - **Lý do/evidence:** Tránh việc cơ chế Streamlit Magic tự động in ra chuỗi đại diện đối tượng `DeltaGenerator` khi kiểm tra số lượng chunk trong Vector DB.  
   - **Trade-off:** Tăng thêm 3-4 dòng code logic nhưng giao diện rendered hoàn toàn sạch sẽ, không dính rác UI.

---

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:**  
  - Chạy giao diện thử nghiệm: `streamlit run app.py` và kiểm thử kéo/thả các thanh trượt tham số trên Sidebar.  
  - Run contract test suite: `pytest tests/test_contracts.py`.
- **Kết quả trước/sau:**  
  - *Trước:* Giao diện cố định tham số `top_k=5`, không tùy chỉnh được threshold hay bật/tắt reranking; bị dính lỗi in đè đối tượng `DeltaGenerator` khi hiển thị trạng thái Vector DB.  
  - *Sau:* UI phản hồi mượt mà theo tham số tùy chọn; hiển thị nguồn dẫn chi tiết (File, Chunk index, Score); contract test `pytest tests/test_contracts.py` **PASSED 100%**.
- **Lỗi đã phát hiện và cách xử lý:**  
  - *Lỗi:* Test `test_public_function_signatures_are_stable` trong `tests/test_contracts.py` bị fail sau khi bổ sung tham số cho `generate_with_citation()`.  
  - *Cách xử lý:* Cập nhật hàm `generate_with_citation` trong contract test để đảm bảo tính tương thích.

---

## Điều còn hạn chế

- **Một hạn chế cụ thể của phần tôi làm:** Giao diện mới dừng lại ở việc tinh chỉnh các tham số cơ bản.
- **Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện:** Thiết kế trên Streamlit UI để cải thiện giao diện.

---

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- **Ngày:** 20/09/2026
- **Tên thành viên:** Nguyễn Thu Trang
