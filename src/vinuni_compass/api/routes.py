"""Transport interface declaration; framework wiring follows structure review."""

ROUTES = {
    "health": "/health",
    "query": "/api/query",
    "models": "/v1/models",
    "chat_completions": "/v1/chat/completions",
}
