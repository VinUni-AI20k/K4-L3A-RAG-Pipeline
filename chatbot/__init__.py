"""Self-contained chatbot for legal Q&A on apartment sale contracts.

Independent from src/task1..task10 (those remain the graded pipeline
exercises). This package implements its own minimal ingest -> chunk ->
embed -> hybrid retrieval -> citation-grounded generation flow so the
chatbot UI (item 9 of docs/STEP_BY_STEP.md) can run end to end.
"""
